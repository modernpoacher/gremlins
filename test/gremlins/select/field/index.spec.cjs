const {
  expect
} = require('chai')

const Field = require('#gremlins/gremlins/select/field')

describe('#gremlins/gremlins/select/field', () => {
  describe('`Field`', () => {
    it('is a function', () => {
      expect(Field)
        .to.be.a('function')
    })
  })
})
