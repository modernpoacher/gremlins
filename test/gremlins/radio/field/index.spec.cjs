const {
  expect
} = require('chai')

const Field = require('#gremlins/gremlins/radio/field')

describe('#gremlins/gremlins/radio/field', () => {
  describe('`Field`', () => {
    it('is a function', () => {
      expect(Field)
        .to.be.a('function')
    })
  })
})
