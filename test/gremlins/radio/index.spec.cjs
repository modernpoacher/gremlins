const {
  expect
} = require('chai')

const Radio = require('#gremlins/gremlins/radio')

describe('#gremlins/gremlins/radio', () => {
  describe('`Radio`', () => {
    it('is a function', () => {
      expect(Radio)
        .to.be.a('function')
    })
  })
})
