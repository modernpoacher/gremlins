const {
  expect
} = require('chai')

const Field = require('#gremlins/gremlins/textarea/field')

describe('#gremlins/gremlins/textarea/field', () => {
  describe('`Field`', () => {
    it('is a function', () => {
      expect(Field)
        .to.be.a('function')
    })
  })
})
