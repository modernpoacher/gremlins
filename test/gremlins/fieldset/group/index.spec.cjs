const {
  expect
} = require('chai')

const Group = require('#gremlins/gremlins/fieldset/group')

describe('#gremlins/gremlins/fieldset/group', () => {
  describe('`Group`', () => {
    it('is a function', () => {
      expect(Group)
        .to.be.a('function')
    })
  })
})
