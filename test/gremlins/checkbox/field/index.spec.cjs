const {
  expect
} = require('chai')

const Field = require('#gremlins/gremlins/checkbox/field')

describe('#gremlins/gremlins/checkbox/field', () => {
  describe('`Field`', () => {
    it('is a function', () => {
      expect(Field)
        .to.be.a('function')
    })
  })
})
