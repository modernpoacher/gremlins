const {
  expect
} = require('chai')

const Checkbox = require('#gremlins/gremlins/checkbox')

describe('#gremlins/gremlins/checkbox', () => {
  describe('`Checkbox`', () => {
    it('is a function', () => {
      expect(Checkbox)
        .to.be.a('function')
    })
  })
})
