import {
  expect
} from 'chai'

import Field from '#gremlins/gremlins/number/field'

describe('#gremlins/gremlins/number/field', () => {
  describe('`Field`', () => {
    it('is a function', () => {
      expect(Field)
        .to.be.a('function')
    })
  })
})
