import {
  expect
} from 'chai'

import Group from '#gremlins/components/group'

describe('#gremlins/components/group', () => {
  describe('`Group`', () => {
    it('is a function', () => {
      expect(Group)
        .to.be.a('function')
    })
  })
})
