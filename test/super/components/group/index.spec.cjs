const {
  expect
} = require('chai')

const Group = require('#gremlins/super/components/group')

describe('#gremlins/super/components/group', () => {
  describe('`Group`', () => {
    it('is a function', () => {
      expect(Group)
        .to.be.a('function')
    })
  })
})
