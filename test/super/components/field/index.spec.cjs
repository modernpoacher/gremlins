const {
  expect
} = require('chai')

const Field = require('#gremlins/super/components/field')

describe('#gremlins/super/components/field', () => {
  describe('`Field`', () => {
    it('is a function', () => {
      expect(Field)
        .to.be.a('function')
    })
  })
})
